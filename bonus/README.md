# Bonus KFS-1

Meme kernel, plus scroll, curseur, couleurs, printk, clavier et 4 tty (F1-F4).

```bash
make run    # QEMU graphique
make test   # meme ISO, boot auto, verifie 42 (depuis la racine : make -C bonus test)
```
